{
  "name": "event-system",
  "version": "1.0.0",
  "description": "POC of a event system based on command pattern",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel app-src -d app --source-maps",
    "watch": "babel app-src -d app --source-maps --watch",
    "debug": "node --nolazy --inspect --inspect-brk=9229 index.js"
  },
  "author": "Helton Costa",
  "license": "MIT",
  "dependencies": {
    "es6-enum": "^1.1.0"
  }
}
